<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper>
	<select id="selectAllBoard" resultType="dto">
  		select * from board
  	</select>
  	
  	<select id="selectOneBoard" parameterType="String" resultType="dto">
  		select * from board where writer = #{writer}
  	</select>

	<insert id="insertBoard" parameterType="dto">
		insert into board 
		values(#{b_no},#{user_no},#{title},#{write_date},#{writer},#{contents},#{like},#{hits})
	</insert>
	
	<update id="updateBoard" parameterType="dto">
  		update user_info set
  		nicname = #{nicname}, email = #{email}
  		where id = #{id}
  	</update>
  	
  	<delete id="deleteBoard" parameterType="String">
  		delete from user_info where id = #{id}
  	</delete>




</mapper>  
  